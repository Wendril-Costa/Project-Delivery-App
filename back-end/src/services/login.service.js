const { User } = require('../database/models');
const { compareHash } = require('../utils/hashMD5');

const login = async ({ email, password }) => {
  
    if (error.type) {
      return error;
    }
    
   ;
  };
  
  module.exports = { login };